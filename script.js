/* const episodes = 1
const gamma = 0.9
const alpha = 0.99
const epsilon = 1
const min_epsilon = 0.1

const q_table = Array.from({ length: 5 }, () => Array(5).fill(0)); */

const env = document.getElementById("canvas");